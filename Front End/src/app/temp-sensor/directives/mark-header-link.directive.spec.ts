import { MarkHeaderLinkDirective } from './mark-header-link.directive';

describe('MarkHeaderLinkDirective', () => {
  it('should create an instance', () => {
    const directive = new MarkHeaderLinkDirective();
    expect(directive).toBeTruthy();
  });
});
